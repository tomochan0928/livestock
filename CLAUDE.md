# CLAUDE.md — 家畜健康管理アプリ

## ファイル・フォルダの保存場所（最優先ルール）

**保存が必要なファイルやフォルダは、`Z:\` （共有ドライブ）に保存する。**

- PC上で残す必要のあるものは `Z:\` 配下に置く。
  - ビルド成果物（APK）… `Z:\家畜健康管理\apk`
  - アプリから書き出したExcel等の記録 … `Z:\家畜健康管理\エクスポート`
  - その他の資料 … `Z:\` 配下の分かりやすいフォルダ
- `C:\` のデスクトップやダウンロードなど、**そのPCの中だけに残る場所には保存しない**。
- 一時ファイル（動作確認用の使い捨て）は対象外。残す必要が出た時点で `Z:\` へ移す。
- `Z:\` につながらないときは、勝手に別の場所へ保存せず、接続を直してから保存する。

※ Androidアプリ本体のデータは端末内（localStorage）に保存される仕様で、これは変更しない。
　 PCに取り出したファイル（APK・エクスポートしたExcelなど）が上のルールの対象。

## リポジトリ構成

- `livestock_health_v5.html` … アプリ本体（オフライン動作するHTML1枚）
- `app/` … Androidアプリ（WebViewで上記HTMLを表示）
- `setup.sh` … HTMLを `app/src/main/assets/www/` に配置する
- ビルドは GitHub Actions か Android Studio（出力: `app/build/outputs/apk/`）
